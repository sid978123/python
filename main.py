# print("Hello, World!")
# print("Hello:" ,1,2 , sep='/')
# print("hello" , end='-')
# print("man")

# print(True , False,None)

# print([1 , 2, 34,4]) #list
# print((1 , 2, 3,4, 5, 5)) #tuple
# print({1, 2, 3, 4, 5}) #sets
# print({"name":"sid "  , "age " : 23}) #dict


#type function
# print(type(1))


#variables 

# name = "sid"
# print(name)

# age = 12
# print(age)

# a=b=c=4 
# print( a , b, c)

#python support dynamic typing because we don't need to declare the type of variable before using it. we can change the type of variable at any time.

#other languages support static typing because we need to declare the type of variable before using it. we can't change the type of variable at any time.



#keywords in python and identifiers

first_name = "sid" 



fnum = input('Enter your number :')
snum = input('Enter your first number')

result = int(fnum) + int(snum)
print(result)


#Type conversion
 #implicit and explicit type conversion
    # implicit type conversion is done by the interpreter automatically when we perform an operation on two different types of data. for example, when we add an integer and a float, the interpreter will convert the integer to a float before performing the addition.

    # explicit type conversion is done by the programmer using built-in functions like int(), float(), str(), etc. for example, if we want to convert a string to an integer, we can use the int() function.

# name = "sid"
name1 = input("Enter your name ")
name2 = input("Enter Your 2nd name ")
result = name1 + " " + name2
print(result  )

type(result)


#Litterals    --->>   in python are the values that we use in our code. they can be of different types like string, integer, float, boolean, etc. for example, "hello" is a string literal, 123 is an integer literal, 3.14 is a float literal, and True is a boolean literal. literals are used to represent data in our code and can be assigned to variables or used directly in expressions.


a = 0b1010 #binary literal
b = 0o12 #octal literal
c = 0x1A #hexadecimal literal

d = float_2 = 1.5e2
print( a , b , c ,d)

#Booleans


k = None
a = 32
b = 32
print(a , b)


